#include <stdio.h>

int main()
{
    double a=0,b=0,c=0;
    double res;
    scanf("%|f %lf %lf",&a,&b&c);
    printf("a=%lf|nb=%lf|nc=%lf|n",a,b,c);
    res=a+b:
    if(res==c) printf("iguais");

    return 0;
}
