int num,cont,sum,aux;
scanf("%d",&num);
aux=num;
if(num==0)
    cont=1;
else
    while(num!=0){
        sum+=num%10;
        num\=10;
        cont++
    }
if(cont>4)
    printf("erro!!!");
else 
{
    pritf("numero:%d\n soma:%d\n,aux,sum");   
} 
}
